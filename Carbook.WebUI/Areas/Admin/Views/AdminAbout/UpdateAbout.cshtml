@model UpdateAboutDto
@{
    ViewData["Title"] = "UpdateAbout";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Hakkımızda Güncelleme Sayfası</h4>
                        <p class="card-subtitle mb-4">Hakkımızda ile ilgili bilgileri aşağıdaki kutucuklara girebilirsiniz. Veri girişinden sonra kaydet butonuna tıklayarak güncelleme işlemini tamamlayabilirsiniz.</p>
                        <form method="post">
                            <input type="hidden" name="AboutId" />
                            <div class="form-group">
                                <label for="exampleInputBrand">Başlık</label>
                                <input type="text" class="form-control" value="@Model.Title" name="Title" />
                            </div>
                            <div class="form-group">
                                <label for="exampleInputBrand">Açıklama</label>
                                <input type="text" class="form-control" value="@Model.Description" name="Description" />
                            </div>
                         
                            <div class="form-group">
                                <label for="exampleInputBrand">Video URL</label>
                                <input type="text" class="form-control" value="@Model.ImageUrl" name="ImageUrl" />
                            </div>

                            <button type="submit" class="btn btn-primary waves-effect waves-light">Banneri Kaydet</button>
                        </form>

                    </div>
                </div>

            </div>
        </div>

    </div>

</div>

