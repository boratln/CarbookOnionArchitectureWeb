@model List<GetByBlogIdTagCloudDto>

<div class="sidebar-box ftco-animate">
    <h3>Etiket Bulutları</h3>
    <div class="tagcloud">
        @foreach(var data in Model)
        {
            <a href="#" class="tag-cloud-link">@data.Title</a>

        }
    
    </div>
</div>
